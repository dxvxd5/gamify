/* Font definitions */
@font-face {
  font-family: 'Poppins';
  font-style: normal;
  font-weight: bold;
  src: local('Poppins'),
    url(../../assets/fonts/Poppins-Bold.ttf) format('truetype');
}
@font-face {
  font-family: 'Bahnschrift';
  font-style: normal;
  font-weight: normal;
  src: local('Bahnschrift'),
    url(../../assets/fonts/Bahnschrift-Regular.ttf) format('truetype');
}

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
    'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
    sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

code {
  font-family: source-code-pro, Menlo, Monaco, Consolas, 'Courier New',
    monospace;
}

/* Use the entire viewport */
html,
body {
  height: 100vh;
  margin: 0;
  padding: 0;
}

#root,
#appRoot {
  height: 100%;
  max-height: 100%;
  background-image: url('../../assets/images/assets.png');
}

/* Override the body height applied by Swirl popup. */
body.swal2-height-auto {
  height: 100vh !important;
}

#gridWrapper {
  display: grid;
  gap: 20px;
  grid-template-columns: 1fr 6fr;
  grid-template-areas: 'sd ct';

  height: 100%;
}

#sidebar {
  grid-area: sd; /* sidebar */
}

.container {
  grid-area: ct; /* content */

  display: grid;
  /* gap: 45px; */
  grid-template-rows: 0.2fr 0.5fr 0.2fr 1fr 0.2fr 1.5fr 0.2fr 0.3fr 0.2fr;
  grid-template-areas:
    '.'
    'wm'
    '.'
    'em'
    '.'
    'ca'
    '.'
    'st'
    '.';

  height: 100vh;
}

.container .welcome-message {
  grid-area: wm; /* welcome message */
  height: 100%;

  /* center the text in the div */
  flex-direction: column;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;

  font-size: 3vw;
  font-family: Poppins;
  font-style: normal;
  font-weight: bold;
  text-transform: uppercase;
  color: #29735e;

  /* border: 1px solid black; */
}
.container .roomid-message {
  grid-area: wm; /* room ID message */
  height: 100%;

  /* center the text in the div */
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  margin-top: 40px;

  font-size: 1.7vw;
  font-family: Poppins;
  font-style: normal;
  font-weight: bold;
  text-transform: uppercase;
  color: #29735e;

  /* border: 1px solid black; */
}

.container .engaging-message {
  grid-area: em; /* engaging message */
  height: 100%;

  /* center the text in the div */
  display: flex;
  align-items: center;
  justify-content: flex-start;
  text-align: center;

  font-family: Poppins;
  font-style: normal;
  font-weight: bold;
  letter-spacing: 0.01em;
  text-transform: uppercase;
  color: #729e82;
  font-size: 3vw;
  margin-left: 60px;

  /* border: 1px solid black; */
}

.container .choose-area {
  grid-area: ca; /* choose area */
  height: 100%;

  display: flex;
  flex-direction: row;
  align-items: flex-start;
  justify-content: space-evenly;

  /* border: 1px solid black; */
}

.result-header-message {
  grid-area: wm; /* welcome message */

  /* center the text in the div */

  align-items: center;
  justify-content: center;
  text-align: center;
  display: center;
  margin-top: 30px;

  font-size: 50px;
  font-family: Poppins;
  font-style: normal;
  font-weight: bold;
  text-transform: uppercase;
  color: #29735e;

  /* border: 1px solid black; */
}

.result-message {
  grid-area: wm; /* welcome message */

  /* center the text in the div */

  align-items: center;
  justify-content: center;
  text-align: center;
  display: grid;
  margin-top: 300px;

  font-size: 2em;
  font-family: Poppins;
  font-style: normal;
  font-weight: bold;
  text-transform: uppercase;
  color: #29735e;

  /* border: 1px solid black; */
}

.container .choose-area button {
  background: #f7f7f8;
  cursor: pointer;
  border: 0;

  font-family: Poppins;
  font-style: normal;
  font-weight: bold;
  text-align: center;
  letter-spacing: 0.01em;
  text-transform: capitalize;
  font-size: 2vw;

  color: #779895;

  padding: 70px 40px;

  box-shadow: 0px 4px 7px rgba(0, 0, 0, 0.25);
  border-radius: 17px;
}

.room-message {
  grid-area: wm; /* welcome message */
  height: 100%;

  /* center the text in the div */
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  margin-top: 150px;

  font-size: 1.7vw;
  font-family: Poppins;
  font-style: normal;
  font-weight: bold;
  text-transform: uppercase;
  color: #29735e97;
}

#rockHand-background {
  position: absolute;
  height: 95%;
  right: 50px;
  top: 10px;
  bottom: 10px;
  /* filter: blur(2px); */
  z-index: -1;
  display: none;
}

@keyframes pop-up {
  from {
    transform: scale(0);
  }
  75% {
    transform: scale(1.2);
  }
  to {
    transform: scale(1);
  }
}
